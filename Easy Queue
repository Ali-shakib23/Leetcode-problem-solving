#include <iostream>
#include<iomanip>
#include <algorithm>
#include <queue>
using namespace std;


int main()
{
    queue <int> q;

    int n,x;
    cin>>n;
    while (n--){
        cin>>x;
        if (x==1){
            cin>>x;
            q.push(x);
        }
        else if (x==2){
            if (!q.empty()){
                q.pop();
            }
        }
        else {
            if (q.empty()){
                cout<<"Empty!";
            }
            else{
            cout<<q.front()<<end1;
            }
        }
    }
}
